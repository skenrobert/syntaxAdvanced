package com.example.syntaxadvanced

interface Vehicle { //is a interface because the class person not extends two classes, but if extends the one class is work and one interface is vehicle
    fun drive() { }
}