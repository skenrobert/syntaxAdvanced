package com.example.syntaxadvanced

abstract class Work { //is a class because the class person not extends two classes, but if extends the one class is work and one interface is vehicle
    abstract fun goToWork()
}